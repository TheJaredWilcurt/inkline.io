import View from '@components/View';
import { IButton, IHeader } from 'inkline';

export default {
    name: 'HeaderView',
    extends: View,
    components: {
        IButton,
        IHeader
    }
};
