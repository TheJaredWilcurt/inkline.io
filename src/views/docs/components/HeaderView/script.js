import View from '@components/View';
import { IButton, IHeader } from '@inkline/inkline';

export default {
    name: 'HeaderView',
    extends: View,
    components: {
        IButton,
        IHeader
    }
};
