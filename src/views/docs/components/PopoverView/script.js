import View from '@components/View';
import { IButton, IPopover } from 'inkline';

export default {
    name: 'PopoverView',
    extends: View,
    components: {
        IButton,
        IPopover,
    }
};
