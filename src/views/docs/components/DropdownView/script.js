import View from '@components/View';
import { IButton, IDropdown } from '@inkline';

export default {
    name: 'DropdownView',
    extends: View,
    components: {
        IButton,
        IDropdown
    }
};
