import View from '@components/View';
import { IButton, INav, INavItem } from '@inkline/inkline';

export default {
    name: 'NavView',
    extends: View,
    components: {
        IButton,
        INav,
        INavItem
    }
};
