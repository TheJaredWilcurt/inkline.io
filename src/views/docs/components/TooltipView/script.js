import View from '@components/View';
import { IButton, ITooltip } from '@inkline/inkline';

export default {
    name: 'TooltipView',
    extends: View,
    components: {
        IButton,
        ITooltip,
    }
};
