import View from '@components/View';
import { IButton, ITooltip } from 'inkline';

export default {
    name: 'TooltipView',
    extends: View,
    components: {
        IButton,
        ITooltip,
    }
};
