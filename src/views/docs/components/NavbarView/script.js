import View from '@components/View';
import { IButton, INavbar, INavbarBrand } from 'inkline';

export default {
    name: 'NavbarView',
    extends: View,
    components: {
        IButton,
        INavbar,
        INavbarBrand
    }
};
