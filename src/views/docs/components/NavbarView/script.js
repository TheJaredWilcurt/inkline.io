import View from '@components/View';
import { IButton, INav, INavItem, INavbar, INavbarBrand } from 'inkline';

export default {
    name: 'NavbarView',
    extends: View,
    components: {
        IButton,
        INav,
        INavItem,
        INavbar,
        INavbarBrand
    }
};
