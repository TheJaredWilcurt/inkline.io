import View from '@components/View';
import { IProgress, IProgressBar } from 'inkline';

export default {
    name: 'ProgressView',
    extends: View,
    components: {
        IProgress,
        IProgressBar,
    }
};
