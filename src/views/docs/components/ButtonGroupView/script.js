import View from '@components/View';
import { IButton, IButtonGroup } from '@inkline';

export default {
    name: 'ButtonGroupView',
    extends: View,
    components: {
        IButton,
        IButtonGroup
    }
};
