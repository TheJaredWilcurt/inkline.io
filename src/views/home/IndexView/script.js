import View from '@components/View';
import SiteNavigation from '@components/SiteNavigation';

export default {
    name: 'HomeView',
    extends: View,
    components: {
        SiteNavigation
    }
};
